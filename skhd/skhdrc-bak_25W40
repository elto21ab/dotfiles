# https://github.com/koekeishiya/skhd/issues/1
# + between modifier keys, and - between normalkeys
# useless: fn == 0x3F (hold) and 0xB3 (quick/release) <==> 0x3F  |  hyper == 0x3C
# esc == 0x35
# ralt - + : yabai -m query --windows
# ralt + shift - + : yabai -m signal --list

ralt - down : yabai -m window --swap south || yabai -m window --focus north
ralt - up : yabai -m window --swap north || yabai -m window --focus south
ralt - left : yabai -m window --swap west || yabai -m window --focus east
ralt - right : yabai -m window --swap east || yabai -m window --focus west

ralt - f : yabai -m window --toggle float
ralt - s : yabai -m window --toggle sticky
ralt - p : yabai -m window --toggle sticky --toggle pip

ralt - e : yabai -m space --equalize 
ralt - b : yabai -m space --balance
ralt - r : yabai --restart-service && skhd --restart-service # 0 == 0x1D

# ralt - 0x24 : yabai -m window --display recent; yabai -m display --focus recent # enter <== throw window to other display
# ralt - 0 : open -na 'zen browser'

ctrl - 0x35 : yabai -m space --toggle show-desktop # 0x35 == esc | fn - z


ctrl - 1 : yabai -m space --focus 1
ctrl - 2 : yabai -m space --focus 2
ctrl - 3 : yabai -m space --focus 3
ctrl - 4 : yabai -m space --focus 4
ctrl - 5 : yabai -m space --focus 5

# lalt - m: yabai -m space --create
# lalt - 0x30 : yabai -m space --focus recent # 0x30 == tab

###################### WINDOW --> SPACE & MOUSE FOLLOWS ###############################
### Move/resize if float/tiled window
rcmd - right : if [ "$(yabai -m query --windows --window | jq -r '.["is-floating"]')" = "true" ]; then yabai -m window --move rel:100:0; else yabai -m window --resize right:100:0; fi
rcmd - left  : if [ "$(yabai -m query --windows --window | jq -r '.["is-floating"]')" = "true" ]; then yabai -m window --move rel:-100:0; else yabai -m window --resize left:-100:0; fi
rcmd - up    : if [ "$(yabai -m query --windows --window | jq -r '.["is-floating"]')" = "true" ]; then yabai -m window --move rel:0:-100; else yabai -m window --resize top:0:-100; fi
rcmd - down  : if [ "$(yabai -m query --windows --window | jq -r '.["is-floating"]')" = "true" ]; then yabai -m window --move rel:0:100; else yabai -m window --resize bottom:0:100; fi

# send window to space and follow focus
# rcmd + ralt - left : (yabai -m window --space prev && yabai -m space --focus prev) || (yabai -m window --space last && yabai -m space --focus last)
# rcmd + ralt - right : (yabai -m window --space next && yabai -m space --focus next) || (yabai -m window --space first && yabai -m space --focus first)

# # Move space
# ctrl + shift - left : (yabai -m space --move prev) || (yabai -m space --swap first)
# ctrl + shift - right : yabai -m space --move next || yabai -m space --swap first
# # ctrl - left : yabai -m space --focus prev || yabai -m space --focus last
# # ctrl - right : yabai -m space --focus next || yabai -m space --focus first


# # WORKS 2025-09-29 w/o follow focus
# # send window to specific space and follow focus
# ralt - 1 : yabai -m window --space 1 && yabai -m space --focus 1
# ralt - 2 : yabai -m window --space 2 && yabai -m space --focus 2
# ralt - 3 : yabai -m window --space 3 && yabai -m space --focus 3
# ralt - 4 : yabai -m window --space 4 && yabai -m space --focus 4
# ralt - 5 : yabai -m window --space 5 && yabai -m space --focus 5
#ralt - 0x32 : yabai -m window --space last && yabai -m space --focus 9
# WORKS 2025-09-29



# ctrl - n : yabai -m window --focus stack.next || yabai -m window --focus stack.prev
# # Stack Navigation: shift + ctrl - {n, p}
# # ctrl - p : yabai -m window --focus stack.prev

# # create new space and move window to it (? or create new space to the left of the current one ?)
# ctrl + alt - n : yabai -m space --create && \
#     index="$(yabai -m query --spaces --display | jq 'map(select(."is-native-fullscreen" == false))[-1].index')" && \
#     yabai -m window --space "${index}" && \
#     yabai -m space --focus "${index}"

# # close all *EMPTY* spaces, incl. current space, and???
# ctrl + alt - backspace :
# 	index="$(yabai -m query --spaces --display | jq 'map(select(."has-focus" == true))[0].index')" && \
# 	yabai -m space --destroy && \
# 	yabai -m space --focus "${index}" || yabai -m space --focus "$(($index - 1))" \
# 	&& yabai -m query --spaces --display | \
# 	jq -re 'map(select(."is-native-fullscreen" == false)) | length > 1' \
# 	&& yabai -m query --spaces | \
# 	jq -re 'map(select(."windows" == []).index) | reverse | .[] ' | \
# 	xargs -I % sh -c 'yabai -m space % --destroy'
